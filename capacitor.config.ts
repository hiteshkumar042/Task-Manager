import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hitesh.todoapp',
  appName: 'Task Manager',
  webDir: "dist/todoapp/browser"
};

export default config;
